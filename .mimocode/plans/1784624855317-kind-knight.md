# Remove dead overlay + fix stat

## 1. Delete `components/casestudy/CaseStudyOverlay.tsx`
Never imported anywhere — dead code.

## 2. Fix Buna Mahber stat (`components/casestudy/data.ts`)
**Line 76**: Change `0→∞` to `1<em>M+</em>`:
```ts
{ num: "1<em>M+</em>", label: "Digital presence", p: "..." }
```

## Verification
- Buna Mahber stat shows "1M+" instead of infinity symbol
- No import errors from deleted file
